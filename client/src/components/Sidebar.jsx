import React from"react";import{NavLink}from"react-router-dom";import{Activity,ClipboardList,FileClock,LayoutDashboard,PlusCircle,ShieldCheck}from"lucide-react";export default function Sidebar({collapsed,surge}){const links=[["/",LayoutDashboard,"Command Centre"],["/intake",PlusCircle,"New Patient"],["/queue",ClipboardList,"Waiting Queue"],["/audit",FileClock,"Audit Trail"]];return <aside className={`sidebar ${collapsed?"collapsed":""}`}><div className="brand"><div className="brand-mark"><Activity size={22}/></div>{!collapsed&&<div><strong>PatientTriage<span>.ai</span></strong><small>human-in-the-loop ED</small></div>}</div><nav>{links.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>isActive?"nav active":"nav"}><Icon size={19}/>{!collapsed&&<span>{label}</span>}</NavLink>)}</nav>{!collapsed&&<div className="sidebar-bottom"><div className="safety-card"><ShieldCheck size={18}/><div><strong>Safety layer</strong><p>Escalate under uncertainty</p></div></div><div className={`surge-card ${surge?"active":""}`}><span className="pulse"/> {surge?"3× surge simulation":"Normal load"}</div></div>}</aside>}
