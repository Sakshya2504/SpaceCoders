import { AlertTriangle, Activity, Clock3, HeartPulse, Users } from 'lucide-react';
import { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import api from '../api';
import { ActionBadge, EsiBadge } from '../components/Badge';
export default function Dashboard(){const[d,setD]=useState(null);const[m,setM]=useState(null);const load=async()=>{const[a,b]=await Promise.all([api.get('/dashboard/summary'),api.get('/dashboard/metrics')]);setD(a.data.data);setM(b.data.data)};useEffect(()=>{load();const t=setInterval(load,10000);return()=>clearInterval(t)},[]);if(!d)return <div className="page-loading">Loading command centre…</div>;return <div><div className="hero-banner"><div><div className="eyebrow">LIVE EMERGENCY OPERATIONS</div><h2>Safety-first triage orchestration</h2><p>Continuous queue monitoring, transparent recommendations and clinician control.</p></div><strong>{d.surgeMode?'3× SURGE MODE':'NORMAL MODE'}</strong></div><div className="kpi-grid">{[[Users,'Patients Today',d.patientsToday],[Clock3,'Waiting',d.waiting],[AlertTriangle,'Critical / ESI 1–2',d.critical],[HeartPulse,'High Risk',d.highRisk],[Activity,'Reassessment Due',d.reassessmentDue],[HeartPulse,'Avg Confidence',`${d.avgConfidence}%`]].map(([I,l,v])=><div className="kpi-card" key={l}><div className="kpi-icon"><I size={18}/></div><div><div className="kpi-value">{v}</div><div className="kpi-label">{l}</div></div></div>)}</div><div className="content-grid two-col"><section className="panel"><div className="panel-head"><div><h3>Risk distribution</h3><p>Current ESI mix</p></div></div>{d.esi.map(x=><div className="metric-row" key={x.name}><span>{x.name}</span><div className="metric-track"><span style={{width:`${Math.min(100,x.value*10)}%`}}/></div><strong>{x.value}</strong></div>)}</section><section className="panel"><div className="panel-head"><div><h3>Safety logic</h3><p>Decision rules visible to the clinician</p></div></div><div className="activity-item"><AlertTriangle size={15}/><div><strong>Red flag positive → immediate escalation</strong><small>Parallel detectors run independently.</small></div></div><div className="activity-item"><Activity size={15}/><div><strong>Low confidence → abstain & escalate</strong><small>Uncertainty is never silently suppressed.</small></div></div><div className="activity-item"><HeartPulse size={15}/><div><strong>Failure → fail open</strong><small>Manual ESI remains available.</small></div></div></section></div><div className="content-grid two-col"><section className="panel"><div className="panel-head"><div><h3>Recent events</h3><p>{m?.recent?.length||0} latest audit events</p></div><Link className="secondary-button" to="/audit">Open audit</Link></div>{(m?.recent||[]).slice(0,7).map(e=><div className="activity-item" key={e.eventId}><div><strong>{e.eventType.replaceAll('_',' ')}</strong><small>{e.patientId||'SYSTEM'} · {new Date(e.timestamp).toLocaleString()}</small></div></div>)}</section><section className="panel"><div className="panel-head"><div><h3>Round 2 controls</h3><p>Use these for the judging flow</p></div></div><div className="action-buttons"><Link className="primary-button" to="/intake">New patient</Link><Link className="secondary-button" to="/queue">Live queue</Link><Link className="secondary-button" to="/audit">Verify audit</Link></div><div className="success-banner">Synthetic prototype · human-in-the-loop · nurse decides.</div></section></div></div>}
