import React,{useState}from"react";import{Activity,ArrowRight,LockKeyhole,Mail}from"lucide-react";import{api}from"../api";export default function Login({onLogin}){const[email,setEmail]=useState("charge@patienttriage.demo"),[password,setPassword]=useState("demo123"),[error,setError]=useState("");const submit=async e=>{e.preventDefault();setError("");try{const{data}=await api.post("/auth/login",{email,password});localStorage.setItem("pt_token",data.token);localStorage.setItem("pt_user",JSON.stringify(data.user));onLogin(data.user)}catch(err){setError(err.response?.data?.message||"Unable to sign in")}};return <div className="login-page"><div className="login-glow"/><div className="login-card"><div className="brand large"><div className="brand-mark"><Activity size={25}/></div><div><strong>PatientTriage<span>.ai</span></strong><small>ED decision-support layer</small></div></div><div className="login-copy"><h1>Second pair of eyes.<br/><em>Human in control.</em></h1><p>Real-time triage recommendations with confidence, red-flag detection and explainable reasoning.</p></div><form onSubmit={submit}><label>Email<div className="input-wrap"><Mail size={17}/><input value={email} onChange={e=>setEmail(e.target.value)}/></div></label><label>Password<div className="input-wrap"><LockKeyhole size={17}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div></label>{error&&<div className="error">{error}</div>}<button className="primary full">Enter command centre <ArrowRight size={17}/></button></form><div className="demo-hint"><strong>Demo:</strong> charge@patienttriage.demo / demo123</div></div></div>}
