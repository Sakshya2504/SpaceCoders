import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import api from '../api';
export default function Login(){const navigate=useNavigate();const[form,setForm]=useState({email:'charge@patienttriage.demo',password:'demo123'});const[error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');try{const r=await api.post('/auth/login',form);localStorage.setItem('pt_token',r.data.data.token);localStorage.setItem('pt_user',JSON.stringify(r.data.data.user));navigate('/dashboard');}catch(err){setError(err.response?.data?.message||'Unable to sign in');}};return <div className="login-page"><div className="login-card"><div className="login-brand"><div className="brand-mark">✚</div><div><strong>PatientTriage<span>.ai</span></strong><small>ED decision-support layer</small></div></div><div className="login-kicker">ACCENTURE INNOVATION CHALLENGE 2026 · ROUND 2</div><h1>ED Command Centre</h1><p className="muted">Real-time triage recommendations with confidence, red-flag detection and explainable reasoning. The nurse remains in control.</p><form onSubmit={submit} className="form-stack"><label>Email<input value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="error-banner">{error}</div>}<button className="primary-button" type="submit">Enter command centre</button></form><div className="demo-login">Demo: charge@patienttriage.demo / demo123</div></div></div>}
