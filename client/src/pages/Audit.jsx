import { CheckCircle2, ShieldCheck, XCircle } from 'lucide-react';
import { useEffect, useState } from 'react';
import api from '../api';
export default function Audit(){const[logs,setLogs]=useState([]);const[v,setV]=useState(null);const load=()=>api.get('/audit').then(r=>setLogs(r.data.data||[]));useEffect(()=>{load()},[]);const verify=()=>api.get('/audit/verify').then(r=>setV(r.data.data));return <div><div className="hero-small"><div><div className="eyebrow">GOVERNANCE & TRACEABILITY</div><h2>Audit trail</h2><p>Every recommendation, reassessment and override is linked with SHA-256.</p></div><button className="primary-button" onClick={verify}><ShieldCheck size={15}/> Verify chain</button></div>{v&&<div className={`verify-card ${v.valid?'valid':'invalid'}`}>{v.valid?<CheckCircle2/>:<XCircle/>}<div><strong>{v.valid?'CHAIN VALID':'CHAIN INVALID'}</strong><p>{v.valid?`${v.events} events verified from GENESIS.`:v.reason}</p></div></div>}<section className="panel table-panel"><div className="table-scroll"><table><thead><tr><th>Time</th><th>Event</th><th>Patient</th><th>Actor</th><th>Hash</th><th>Previous</th></tr></thead><tbody>{logs.map(e=><tr key={e.eventId}><td>{new Date(e.timestamp).toLocaleString()}</td><td><span className="event-pill">{e.eventType.replaceAll('_',' ')}</span></td><td>{e.patientId||'SYSTEM'}</td><td>{e.actorRole}</td><td><code>{e.hash?.slice(0,16)}…</code></td><td><code>{e.previousHash?.slice(0,16)}…</code></td></tr>)}</tbody></table></div></section></div>}
